# f  = c * (9 / 5) + 32

# taking temprature in celsius as input
celsius_temp = float(input("Enter temperature in celsius:"))

# converting it to F
fah_temp = (celsius_temp * 9 /5) + 32

print("Temperature in fahrenheit:", fah_temp)